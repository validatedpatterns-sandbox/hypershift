# Override values for HyperShift 
global:
  useExternalSecrets: true
  s3:
    createBucket: true
    region: us-west-2
    bucketName: jrickard-deploytest
    additionalTags: 
      owner: jrickard
      destroy: true
  
  buildconfig:
    git:
      uri: https://github.com/validatedpatterns-sandbox/hypershift
      ref: hypershift
