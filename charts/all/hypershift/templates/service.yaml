apiVersion: v1
kind: Service
metadata:
  name: {{ include "hypershift.fullname" . }}
  labels:
    {{- include "hypershift.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hypershift.selectorLabels" . | nindent 4 }}
