apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hypershift.fullname" . }}-test-connection"
  labels:
    {{- include "hypershift.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hypershift.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
